import type { Config } from 'tailwindcss';

const config: Config = {
  content: {
    files: ['./index.html', './src/**/*.tsx'],
  },
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
